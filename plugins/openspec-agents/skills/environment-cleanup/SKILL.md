---
name: environment-cleanup
description: 隔离环境清理通用规范——停止残留服务、清理隔离容器/数据库/mock 服务/中间件、删除隔离扫描分析项目。技术栈无关。
capabilities: ["cleanup"]
---

> **项目规范优先**：本 skill 所列约定为推荐标准。若项目已有明确规范且与本 skill 不一致，以项目规范为准。
> 清理以幂等为原则：无残留即通过，不因重复执行报错或产生副作用。

## 停止残留的服务实例

停止本次变更启动的服务实例，禁止误杀其他变更或他人启动的进程：

- 经进程启动的服务：确认归属本变更后停止，按以下顺序判别
- 经容器编排启动的服务：按本变更的隔离标识停止对应 compose 项目

经进程启动的服务按以下顺序确认归属：

1. 优先按启动时记录的进程 PID 停止；停止前核对该 PID 对应进程的启动命令或工作目录仍属于本变更（操作系统可能复用已退出进程的 PID），确认后方可停止
2. 无 PID 记录时，以会话分配的隔离端口（专属本变更）为唯一定位入口：隔离端口无进程占用即视为无本变更服务残留；有占用时查询该进程的工作目录，位于本变更工作目录内（来源：会话分配的隔离上下文，含隔离标识、建议端口、本变更工作目录）方可停止，不在即禁止停止
3. 禁止按启动命令关键字全局搜索进程作为定位手段（本变更工作区与主仓库的启动命令相同，无法区分归属）
4. 无法确认归属本变更的进程（含监听在项目默认端口上的进程）一律不停止、不干扰；目标端口被其他进程占用属于本变更启动失败，应修正端口参数重新以隔离方式启动，禁止为腾出端口而停止任何进程

停止确认以被停止的进程已退出为准；端口随后被其他进程占用不构成本变更未停止（如用户或他人进程占用同一端口），不得据此二次清理。

## 清理隔离环境

按本变更的隔离标识确认无残留的隔离资源后再清理：

- 隔离容器：按隔离标识确认存在性后停止并移除（无残留容器即通过，不因重复清理报错）
- 隔离数据库：确认无残留的隔离数据库实例/数据卷后清理；无残留即通过
- 隔离 mock 服务/中间件：按隔离标识停止并移除对应容器/实例；无残留即通过

幂等判据：隔离标识下无残留容器、无残留隔离数据库、无残留隔离 mock 服务/中间件即为清理完成，重复执行不产生副作用。

## 删除隔离扫描分析项目

本变更的隔离扫描分析项目（如按隔离标识命名的分析项目）在变更结束时删除：

1. 先查询确认项目存在（按隔离标识查询），不存在视为已删除，跳过删除动作
2. 确认存在后调用删除接口删除该项目（如平台管理接口的 delete 操作）
3. 删除接口返回「不存在」错误视为已删除（幂等：删除动作可重复执行）

删除后如需重新扫描，由「先查询后创建」的逻辑自动重建（创建前先查存在性，不存在才创建，避免重复创建报错），无需人工干预。

## 清理后自检

清理完成后自检以下各项，全部无残留即通过：

- 无残留的服务进程（以本变更启动的进程已退出为准，端口被其他进程占用不构成本变更未停止）
- 无残留的隔离容器（隔离标识下 compose ps 为空）
- 无残留的隔离 mock 服务/中间件（按隔离标识查询不存在或容器已移除）
- 无残留的隔离扫描分析项目（按隔离标识查询不存在）
